from __future__ import annotations

def scan() -> list[dict]:
    return [
        {"symbol": "AAPL", "theme": "post_earnings_drift", "score": 0.64, "note": "Follow-through profile remains constructive"}
    ]
