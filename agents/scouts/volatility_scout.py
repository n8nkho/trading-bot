from __future__ import annotations

def scan() -> list[dict]:
    return [
        {"symbol": "IWM", "theme": "volatility_dislocation", "score": 0.66, "note": "High vol regime favors tactical sizing"}
    ]
